{
  "name": "@lidofinance/lido-sdk-stake",
  "version": "0.0.1",
  "main": "./dist/cjs/index.js",
  "module": "./dist/esm/index.js",
  "types": "./dist/types/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "typecheck": "tsc && tsc-alias",
    "build": "yarn clean && yarn build:cjs && yarn build:esm && yarn build:types",
    "publish:package": "yarn build && npm publish --registry http://localhost:4873/",
    "build:cjs": "tsc --project tsconfig.build.json --module commonjs --outDir ./dist/cjs --removeComments --verbatimModuleSyntax false && echo > ./dist/cjs/package.json '{\"type\":\"commonjs\"}'",
    "build:esm": "tsc --project tsconfig.build.json --module es2015 --outDir ./dist/esm && echo > ./dist/esm/package.json '{\"type\":\"module\",\"sideEffects\":false}'",
    "build:types": "tsc --project tsconfig.build.json --module esnext --declarationDir ./dist/types --emitDeclarationOnly --declaration --declarationMap",
    "prepublishOnly": "node scripts/updateVersion.js",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@lido-sdk/constants": "^3.1.0",
    "@lido-sdk/contracts": "^3.0.2",
    "@lidofinance/lido-sdk-core": "^0.0.1",
    "tiny-invariant": "^1.3.1",
    "viem": "^1.6.4"
  },
  "peerDependencies": {},
  "devDependencies": {
    "@types/fs-extra": "^11.0.1",
    "esbuild-plugin-tsc": "^0.4.0",
    "fs-extra": "^11.1.1",
    "rimraf": "^5.0.1",
    "tsc-alias": "^1.8.7",
    "typescript": "5.1.6"
  }
}
